# LTV Archive

Long-term validation artifacts should be stored together:

- signed document;
- detached signature or embedded signed PDF;
- signer certificate chain;
- timestamp token;
- OCSP response or CRL snapshot;
- audit event reference;
- manifest with hashes for every artifact.
